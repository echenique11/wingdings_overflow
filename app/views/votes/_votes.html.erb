
<%= form_tag votes_path do |f| %>
  <div>
    <%= hidden_field_tag "score", 1 %>
    <%= hidden_field_tag type, id %>
    <%= submit_tag "Vote Up" %>
  </div>
<% end %>

<%= form_tag votes_path do |f| %>
  <div>
    <%= hidden_field_tag "score", -1  %>
    <%= hidden_field_tag type, id %>
    <%= submit_tag "Vote Down" %>
  </div>
<% end %>

